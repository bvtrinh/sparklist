<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%- include ('../partials/header.ejs') %>
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Home</title>
</head>

<body>
  <div class="container">
    <h1>
      <% if(user) { %> 
       <p>Welcome to SparkList <%= user.fname + ' ' + user.lname %> </p>
       <a href="profile"><button type="button" class="btn btn-primary" name="profile">My Profile</button></a>
      <% } else { %>
        <p>Please sign in or register to continue</p>
        <a href="login"><button type="button" class="btn btn-primary" name="login">Login</button></a>
        <a href="register"><button type="button" class="btn btn-primary" name="register">Register</button></a>
      <% } %>
    </h1>
  </div>

  <%- include ('../partials/endBody.ejs') %>
</body>


</html>
